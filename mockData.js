import jsonServer from './jsonServer';
export const getUser = jkh
const handleSubmit=async(e)=>{
    e.preventDefault();
    if(editngUserid){
        const respone = await axios.put('$(baseUrl}/$(editingUserId}',{name,email});
        SpeechSynthesisUtterance(users.map(user =>user.id===))
    }
}import react from 'react';
import UserForm from ',/components/useform';
import UserList from ',/components/UserList';
function app(){
    return(
        <div>
            <userform/>
            
        </div>
    )
}